spring:
  application:
    name: careconnect
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/careconnect}
    username: ${SPRING_DATASOURCE_USERNAME:careconnect}
    password: ${SPRING_DATASOURCE_PASSWORD:careconnect_dev}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:careconnect-dev-secret-key-change-in-production-min-256-bits}
    access-token-expiry-ms: 86400000
    refresh-token-expiry-ms: 604800000
